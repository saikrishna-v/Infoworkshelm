apiVersion: v1
kind: Service
metadata:
  name: {{ template "infoworks.fullname" . }}-ui
  namespace: {{ template "infoworks.namespace" . }}
  labels:
    app: {{ template "infoworks.fullname" . }}-ui
spec:
  type: {{ .Values.infoworks-ui.service.type }}
  ports:
    - port: {{ .Values.infoworks-ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "infoworks.fullname" . }}-ui
